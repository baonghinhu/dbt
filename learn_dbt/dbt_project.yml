name: learn_dbt
version: '1.0.0'
config-version: 2

profile: dbt_project

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  learn_dbt:
    brozon:
      +materialized: table
      +schema: brozon
    silver:
      +materialized: table
      +schema: silver